{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "streamlit run legal_bot_complete.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
  }
}